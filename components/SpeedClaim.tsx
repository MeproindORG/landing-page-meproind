import Reveal from "./Reveal";

export default function SpeedClaim() {
  return (
    <section className="claim" style={{ background: "var(--o)" }}>
      <Reveal className="wrap">
        <div className="pre">Trabaja</div>
        <div className="big">100X</div>
        <div className="sub">veces más rápido que el método tradicional</div>
      </Reveal>
    </section>
  );
}
